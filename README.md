# rain-net
网络

2025.12.22

本来想着用pluginer来代替caddy，实现插件机制。现在发现，插件机制是coreDNS开发的，使用的是caddy的服务启动监听机制，因此pluginer命名不对。

构造链式插件
2025.12.23

额,名字没问题。caddy会先加载所有插件存储在	plugins = make(map[string]map[string]Plugin) 中,然后使用的时候,根据配置文件,把插件加载到Context.Plugin中,在serveDNS的时候构造链式插件


## Star代理转发协议

2026.2.26
目前以及完成http和socks的正向代理,使用浏览器配置可以正常代理,代码结构没有整理。
后续可以开发多路复用的正向代理,反向代理(客户端在内网,连接服务端,浏览器配置地址后可以访问内网的服务),端口映射(服务端的端口映射为客户端的内网服务端口)
代理配置应该设置在服务端,类似于nsp,frp在客户端修改配置不方便。如果提供给用户服务,即可做成一个提供用户内网穿透服务的产品。

DNS线路匹配,链路选择,限速限评,接口配置与查询,缓存监听与同步